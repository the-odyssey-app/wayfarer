{
  "name": "wayfarer-proxy",
  "version": "1.0.0",
  "description": "A simple proxy for Google Places API to work around Nakama httpRequest issue.",
  "main": "api/places-search.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  },
  "note": "Using native fetch (Node 18+) for connection pooling and keep-alive support"
}
